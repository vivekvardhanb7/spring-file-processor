# Application Name
spring.application.name=spring-file-processor

# Server Configuration
server.port=8080

# File Upload Limits (Increase if needed)
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Directory to Store Output JSON Files
file.output.json-location=./test-files/output/
